{
  "name": "healthcore-medical-operations-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "HealthCore | Healthcare Intelligence & Operations Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
